prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0011
  category: TEST_RESULTS
  title: Refactor Radar bounded-output and signature tests
  capture:
    paths:
      - evidence/prd/PRD-0005/refactor_radar/
  verification:
    commands:
      - id: CMD-0001
        shell: bash
        command: |
          set -euo pipefail
          cargo test --package apm2-xtask --lib refactor_radar::tests
        expected_exit_code: 0
        cwd: .
        timeout_seconds: 600
        network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
        - "Radar reports must not emit per-file noise beyond the configured bounds; tests assert hard caps."
