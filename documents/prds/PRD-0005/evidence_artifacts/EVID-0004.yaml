prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0004
  category: LINT_REPORTS
  title: RFC and ticket lint extension reports
  capture:
    paths:
      - evidence/prd/PRD-0005/lint/
      - documents/rfcs/
      - documents/work/tickets/
  verification:
    commands:
      - id: CMD-0001
        shell: bash
        command: |
          set -euo pipefail
          apm2 factory lint --scope rfc --scope tickets
        expected_exit_code: 0
        cwd: .
        timeout_seconds: 600
        network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
        - "Lint reports must omit secrets and must not include full prompt transcripts for external models."
