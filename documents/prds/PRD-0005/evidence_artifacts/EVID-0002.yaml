prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0002
  category: TEST_RESULTS
  title: CCP generation and atlas stability tests
  capture:
    paths:
      - evidence/prd/PRD-0005/ccp/
      - crates/
  verification:
    commands:
      - id: CMD-0001
        shell: bash
        command: |
          set -euo pipefail
          cargo test --package apm2-xtask --lib ccp::tests
        expected_exit_code: 0
        cwd: .
        timeout_seconds: 600
        network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
        - "Atlas artifacts must not embed local usernames or absolute paths; tests assert portability."
