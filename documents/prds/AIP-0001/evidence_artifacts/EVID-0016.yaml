prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: 2
  id: "EVID-0016"
  category: "TEST_RESULTS"
  title: "Agent I/O Test"
  capture:
    paths:
      - "tests/agent_io.rs"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --test agent_io"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 60
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "agent_io"
  data:
    classification: "SENSITIVE"
    redaction:
      required: true
      guidance:
        - "Redact any agent input/output data that may contain sensitive information"
