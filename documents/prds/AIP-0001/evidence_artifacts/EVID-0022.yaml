prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0022"
  category: "TEST_RESULTS"
  title: "WatchSession Streaming Test"
  capture:
    paths:
      - "tests/watch_session.rs"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --test watch_session"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 60
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "watch_session"
  data:
    classification: "SENSITIVE"
    redaction:
      required: true
      guidance:
        - "Redact any session event data that may contain sensitive information"
