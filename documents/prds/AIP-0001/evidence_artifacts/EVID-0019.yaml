prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0019"
  category: "TEST_RESULTS"
  title: "Session Capture Test"
  capture:
    paths:
      - "tests/session_capture.rs"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --test session_capture"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 60
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "session_capture"
  data:
    classification: "SENSITIVE"
    redaction:
      required: true
      guidance:
        - "Redact any session data that may contain sensitive information"
