prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-COORD-0004"
  category: "INTEGRATION_TEST_RESULTS"
  title: "Work freshness check tests"
  description: |
    Validates that work state is checked immediately before session spawn using
    ledger event ordering for consistency. Verifies proper handling of stale
    work items.
  capture:
    paths:
      - "evidence/coordination/freshness/"
  verification:
    commands:
      - id: "CMD-COORD-0004-01"
        shell: "bash"
        command: "cargo test --package apm2-core --lib coordination::controller::tests::freshness_check"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 300
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Test verifies work state check before spawn"
          - "Test verifies skip on completed work"
          - "Test verifies skip on invalid work state"
      - id: "CMD-COORD-0004-02"
        shell: "bash"
        command: "cargo test --package apm2-core --lib coordination::controller::tests::event_ordering"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 300
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Test verifies ledger sequence consistency"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
