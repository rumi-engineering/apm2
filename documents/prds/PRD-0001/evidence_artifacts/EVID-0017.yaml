prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0017
  category: REPRO_STEPS
  title: Recovery and rollback runbooks with executed output logs
  capture:
    paths:
    - ops/recovery/
  verification:
    commands:
    - id: CMD-0017
      shell: bash
      command: ./scripts/evidence/verify_recovery_runbooks.sh --execute smoke
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 900
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Runbook logs must redact secrets; do not embed environment-specific tokens.
