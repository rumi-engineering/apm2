prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0014
  category: SECURITY_SCAN_RESULTS
  title: Security scan results (SAST/dependency) for kernel codebase
  capture:
    paths:
    - security/scans/
  verification:
    commands:
    - id: CMD-0014
      shell: bash
      command: ./scripts/evidence/verify_security_scans.sh --suite kernel
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 1200
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Do not publish vulnerable dependency paths beyond what is necessary; redact
        commit SHAs if needed.
