prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0020
  category: CHANGELOG_ENTRIES
  title: Versioned release documentation entries (release channels + procedures)
  capture:
    paths:
    - documents/releases/
  verification:
    commands:
    - id: CMD-0020
      shell: bash
      command: test -f documents/releases/README.md && rg -n "vX\\.Y\\.Z" documents/releases/RELEASE_CHANNELS.md
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 300
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Documentation must not include secrets; ensure examples use placeholders or
        synthetic data.
