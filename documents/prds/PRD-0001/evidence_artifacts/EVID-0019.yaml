prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0019
  category: TEST_RESULTS
  title: Accessibility and agent-friendly UX conformance checks for CLI outputs
  capture:
    paths:
    - tests/ux_accessibility/
  verification:
    commands:
    - id: CMD-0019
      shell: bash
      command: ./scripts/evidence/verify_cli_accessibility.sh --mode non_color --json_output
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Captured CLI outputs must redact any sensitive file contents displayed in
        errors.
