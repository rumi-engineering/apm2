prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0012
  category: OBSERVABILITY_TRACES
  title: Tracing capture demonstrating work_id correlation across syscalls and links
  capture:
    paths:
    - observability/traces/
  verification:
    commands:
    - id: CMD-0012
      shell: bash
      command: ./scripts/evidence/verify_trace_correlation.sh --required_fields work_id,lease_id,session_id
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Traces must not include raw prompt content unless explicitly classified and
        redacted.
