prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0011
  category: METRICS_DASHBOARDS
  title: Metrics dashboard export for autonomous factory KPIs
  capture:
    paths:
    - observability/metrics/
  verification:
    commands:
    - id: CMD-0011
      shell: bash
      command: ./scripts/evidence/verify_metrics_exports.sh --kpis factory_kpis
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Ensure dashboard exports exclude PII and secrets; aggregate where possible.
