prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0003
  category: POLICY_CHECK_RESULTS
  title: Policy engine conformance tests (default-deny, allowlists, budgets)
  capture:
    paths:
    - policy/
  verification:
    commands:
    - id: CMD-0003
      shell: bash
      command: ./scripts/evidence/verify_policy_engine.sh --suite policy_conformance
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 900
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Redact any example credentials or hostnames from policy fixtures.
