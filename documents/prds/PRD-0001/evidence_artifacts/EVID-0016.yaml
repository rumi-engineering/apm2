prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0016
  category: SIGNATURES
  title: 'Release provenance: checksums and signatures for binary and schemas'
  capture:
    paths:
    - release/provenance/
  verification:
    commands:
    - id: CMD-0016
      shell: bash
      command: ./scripts/evidence/verify_release_signatures.sh
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Never include private signing keys; store only public keys and signatures.
