prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0008
  category: INTEGRATION_TEST_RESULTS
  title: Wire schema compatibility tests (protobuf) across versions and languages
  capture:
    paths:
    - tests/wire_compat/
  verification:
    commands:
    - id: CMD-0008
      shell: bash
      command: ./scripts/evidence/verify_wire_compat.sh --schema v1 --compat_window
        2
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 1200
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Compatibility fixtures must avoid embedding confidential repo content; use
        generated fixtures.
