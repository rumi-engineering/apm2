prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0004"
  category: "SECURITY_TEST"
  title: "ADV-004: Agent produces GateOutcome without lease"
  description: |
    Adversarial test verifying GateLeaseIssued precondition. Gate executor
    attempts to produce GateOutcome without valid lease; expects rejection
    at ledger ingestion.
  source_acceptance_test: ADV-004
  capture:
    paths:
      - "test/daemon/adversarial/adv_004_gate_outcome_no_lease.rs"
      - "evidence/adv/ADV-004/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- daemon::adversarial::adv_004"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 60
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Daemon must be running"
          - "Ledger ingestion validation enabled"
  assertions:
    - "GateOutcome without lease rejected at ingestion"
    - "DefectRecord(GATE_LEASE_MISSING) emitted"
    - "GateOutcome with valid lease accepted"
    - "Lease binding verified (work_id, gate_id, changeset_digest match)"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
