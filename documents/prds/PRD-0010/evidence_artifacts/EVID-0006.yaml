prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0006"
  category: "SECURITY_TEST"
  title: "ADV-006: Same custody domain author + reviewer"
  description: |
    Adversarial test verifying Separation of Duties enforcement. Test attempts
    to spawn gate executor with custody domain overlapping changeset author;
    expects rejection with SOD_VIOLATION.
  source_acceptance_test: ADV-006
  capture:
    paths:
      - "test/daemon/adversarial/adv_006_sod_violation.rs"
      - "evidence/adv/ADV-006/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- daemon::adversarial::adv_006"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 60
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Daemon must be running"
          - "Key policy with custody domains configured"
  assertions:
    - "Gate executor spawn with overlapping custody domain rejected"
    - "LeaseIssueDenied event emitted with reason=SOD_VIOLATION"
    - "CLI returns non-zero exit code"
    - "Error message includes custody domain violation"
    - "Gate executor spawn with distinct custody domain succeeds"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
