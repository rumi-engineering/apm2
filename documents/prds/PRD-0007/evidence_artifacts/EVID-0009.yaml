prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0009"
  category: "TEST_RESULTS"
  title: "Target profile enforcement tests"
  description: |
    Tests verifying target profile budget and rendering policy enforcement
    during pack compilation and export.
  source_acceptance_test: CAC-AT-0009
  capture:
    paths:
      - "test/cac/target_profile/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-cac -- target_profile::enforcement"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Target profiles defined and available"
  assertions:
    - "TargetProfile budgets are enforced during pack compilation"
    - "Rendering policy is honored for derived payloads"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
