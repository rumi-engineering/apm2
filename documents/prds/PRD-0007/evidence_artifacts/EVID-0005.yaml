prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0005"
  category: "TEST_RESULTS"
  title: "Hermetic consumption enforcement tests"
  description: |
    Tests verifying consumption hermeticity: holons cannot fetch artifacts not
    in ContextPack allowlist, discretionary reads emit PolicyViolation and DefectRecord.
  source_acceptance_test: CAC-AT-0005
  capture:
    paths:
      - "test/cac/consumption/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-cac -- consumption::hermeticity"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Policy profile configured for consumption mode"
  assertions:
    - "Consumption holon cannot fetch artifacts not listed in ContextPack manifest allowlist"
    - "Attempted discretionary read emits PolicyViolation and DefectRecord"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
