prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0004"
  category: "TEST_RESULTS"
  title: "DCP resolution determinism tests"
  description: |
    Tests verifying stable_id resolution to pinned hash is deterministic and
    reconstructable from ledger events.
  source_acceptance_test: CAC-AT-0004
  capture:
    paths:
      - "test/cac/dcp/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-cac -- dcp::resolution_determinism"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Ledger with EvidencePublished events available"
  assertions:
    - "stable_id resolves to pinned hash deterministically from ledger projection"
    - "Resolution can be reconstructed from ledger events"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
