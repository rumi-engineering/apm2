prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0007"
  category: "TEST_RESULTS"
  title: "Capabilities truthing tests"
  description: |
    Tests verifying CapabilityManifest matches actual CLI behavior: manifest
    validates against schema, declared commands are invocable and produce
    declared outputs.
  source_acceptance_test: CAC-AT-0007
  capture:
    paths:
      - "test/cac/capabilities/"
      - "evidence/capability_manifest.json"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-cac -- capabilities::truthing"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "apm2 binary available for selftest"
  assertions:
    - "CapabilityManifest validates against schema"
    - "Declared commands are invocable and produce declared outputs"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
