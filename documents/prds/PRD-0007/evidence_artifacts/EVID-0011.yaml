prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0011"
  category: "TEST_RESULTS"
  title: "Run receipt gating tests"
  description: |
    Tests verifying RunReceipt emission and gating: context_pack_miss triggers
    DefectRecord and run failure by default, RunReceipt records sufficiency
    and budget deltas.
  source_acceptance_test: CAC-AT-0011
  capture:
    paths:
      - "test/cac/run_receipt/"
      - "evidence/run_receipts/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-cac -- run_receipt::gating"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Consumption mode configured with pack enforcement"
  assertions:
    - "context_pack_miss triggers DefectRecord and run failure by default"
    - "RunReceipt records sufficiency and budget deltas"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
