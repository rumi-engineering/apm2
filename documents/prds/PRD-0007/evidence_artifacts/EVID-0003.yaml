prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0003"
  category: "TEST_RESULTS"
  title: "Patch replay protection tests"
  description: |
    Tests verifying replay protection via expected_base_hash: patches with wrong
    hash are rejected, accepted patches emit AdmissionReceipt with proper provenance.
  source_acceptance_test: CAC-AT-0003
  capture:
    paths:
      - "test/cac/patch/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-cac -- patch::replay_protection"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Ledger available for provenance recording"
  assertions:
    - "Patch with wrong expected_base_hash is rejected"
    - "Accepted patch emits AdmissionReceipt referencing base + new hashes"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
