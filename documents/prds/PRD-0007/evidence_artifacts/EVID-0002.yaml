prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0002"
  category: "TEST_RESULTS"
  title: "Schema strictness validation tests"
  description: |
    Tests verifying fail-closed schema validation: unknown fields are rejected,
    artifacts exceeding size/depth limits are rejected, and unevaluatedProperties=false
    is enforced.
  source_acceptance_test: CAC-AT-0002
  capture:
    paths:
      - "test/cac/validation/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-cac -- validation::strictness"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Schema validator configured with strict profile"
  assertions:
    - "Unknown fields are rejected"
    - "Artifacts exceeding size/depth limits are rejected"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
