prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0024"
  category: "INTEGRATION_TEST_RESULTS"
  title: "Risk-Tiered AAT Selection Policy Tests"
  description: |
    Integration tests validating risk-tiered AAT selection policy and selection_policy_id
    binding in GateReceipt payloads.
  source_acceptance_test: FAC-AT-0024
  capture:
    paths:
      - "test/fac/aat_selection_policy/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-fac -- aat::selection_policy"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "HIGH risk always requires AAT"
          - "MED/LOW follow sampling rules with nightly full AAT on main"
  assertions:
    - "GateReceipt payload includes risk_tier and selection_policy_id"
    - "Admission rejects selection_policy_id mismatch"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
