prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0027"
  category: "TEST_RESULTS"
  title: "Invariant-First AAT Spec Validation Tests"
  description: |
    Tests validating invariant-first AAT specification requirements and rejection of
    step-based acceptance predicates.
  source_acceptance_test: FAC-AT-0027
  capture:
    paths:
      - "test/fac/aat_spec_invariants/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-fac -- aat::spec_invariants"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Specs require at least one invariant"
          - "Steps cannot be acceptance predicates"
  assertions:
    - "Invariant declarations are mandatory and machine-checkable"
    - "Step-only specs are rejected"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
