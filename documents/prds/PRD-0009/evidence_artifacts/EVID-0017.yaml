prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0017"
  category: "TEST_RESULTS"
  title: "FAC Canonicalization and Signature Test Vectors"
  description: |
    Tests verifying canonical bytes, sorting rules, and signature verification for
    FAC critical events and receipts.
  source_acceptance_test: FAC-AT-0017
  capture:
    paths:
      - "test/fac/canonicalization/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- canonicalization::vectors"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Golden vectors include GateLeaseIssued, GateRunCompleted, MergeReceipt"
  assertions:
    - "Canonicalization rules produce stable hashes"
    - "Signatures verify against canonical bytes with domain separation"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
