prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0008"
  category: "TEST_RESULTS"
  title: "Echo-Trap Detection Tests"
  description: |
    Tests verifying echo-trap detection and termination when FindingSignature repeats
    without delta within a single GateRun.
  source_acceptance_test: FAC-AT-0008
  capture:
    paths:
      - "test/fac/echo_trap/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- echo_trap::detection"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "GateRun fixtures include repeated FindingSignature sequences"
  assertions:
    - "Echo-trap threshold=3 triggers SessionTerminated rationale=REASONING_DEGENERATION"
    - "DefectRecord(ECHO_TRAP) emitted on detection"
    - "Escalation to OracleRequest or AdjudicationRequested recorded"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
