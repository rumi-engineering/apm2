prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0026"
  category: "INTEGRATION_TEST_RESULTS"
  title: "AAT Lease Binding and Executor Isolation Tests"
  description: |
    Integration tests validating AAT GateLease scope bindings and executor custody
    separation from coordinator/implementer roles.
  source_acceptance_test: FAC-AT-0026
  capture:
    paths:
      - "test/fac/aat_lease_binding/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --package apm2-fac -- aat::lease_binding"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Lease includes view_commitment_hash and rcp_aat_manifest_hash"
          - "Executor custody domain separation enforced"
  assertions:
    - "GateRunCompleted rejects mismatched lease bindings"
    - "Unauthorized custody domains are rejected at lease issuance"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
