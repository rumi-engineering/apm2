prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0010"
  category: "INTEGRATION_TEST_RESULTS"
  title: "Holdout Audit and Evaluator Drift Tests"
  description: |
    Tests verifying FAC-90 holdout audits, drift detection thresholds, and policy
    quarantine behavior.
  source_acceptance_test: FAC-AT-0010
  capture:
    paths:
      - "test/fac/holdout_audit/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- holdout::drift_detection"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Holdout suite fixtures include known-bad ChangeSets"
          - "Policy quarantine fixtures available"
  assertions:
    - "False-negative rate threshold triggers EVALUATOR_DRIFT"
    - "Policy quarantine requires explicit adjudication for admission"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
