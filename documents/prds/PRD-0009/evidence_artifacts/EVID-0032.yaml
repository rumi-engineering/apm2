prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0032"
  category: "INTEGRATION_TEST_RESULTS"
  title: "AAT Harness Sandbox and Egress Control Tests"
  description: |
    Integration tests verifying AAT harness sandboxing, deny-by-default egress,
    and environment allowlisting.
  source_acceptance_test: FAC-AT-0032
  capture:
    paths:
      - "test/fac/aat_harness_sandbox/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- aat::harness_sandbox"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Egress deny enforced"
          - "Env allowlist enforced"
          - "Host mounts blocked"
  assertions:
    - "Sandbox violations are rejected and logged"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
