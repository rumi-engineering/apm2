prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0015"
  category: "CHAOS_TEST_RESULTS"
  title: "FAC Resilience Fault Injection Tests"
  description: |
    Fault injection tests verifying FAC resilience under provider errors, tool
    failures, and projection divergence scenarios.
  source_acceptance_test: FAC-AT-0015
  capture:
    paths:
      - "test/fac/resilience/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- resilience::fault_injection"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 300
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Fault injection harness simulates provider error taxonomy"
  assertions:
    - "Circuit breaker thresholds enforced"
    - "Retry policy adheres to exponential backoff"
    - "Divergence watchdog triggers freeze on simulated tamper"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
