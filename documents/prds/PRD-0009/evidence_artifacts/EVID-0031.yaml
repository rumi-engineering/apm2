prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0031"
  category: "INTEGRATION_TEST_RESULTS"
  title: "AAT Quorum/Audit Enforcement Tests"
  description: |
    Integration tests verifying HIGH-risk quorum receipts and MED-risk audit re-runs
    before admission finalization.
  source_acceptance_test: FAC-AT-0031
  capture:
    paths:
      - "test/fac/aat_quorum_audit/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- aat::quorum_audit"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "HIGH requires two independent receipts"
          - "MED audit re-run required before admission"
  assertions:
    - "Admission blocks without quorum/audit"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
