prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0009"
  category: "INTEGRATION_TEST_RESULTS"
  title: "Conflict-of-Interest (COI) Enforcement Tests"
  description: |
    Tests verifying COI enforcement during lease issuance and waiver ceremony handling.
  source_acceptance_test: FAC-AT-0009
  capture:
    paths:
      - "test/fac/coi_enforcement/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- coi::enforcement"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Lease issuance fixtures include author/executor COI groupings"
          - "Waiver fixtures include bounded scope and time_envelope_ref (HTF)"
  assertions:
    - "Lease issuance rejects executor in same COI group as changeset author"
    - "Waiver path requires explicit adjudication and bounded scope"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
