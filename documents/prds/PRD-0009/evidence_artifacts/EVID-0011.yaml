prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0011"
  category: "POLICY_CHECK_RESULTS"
  title: "Ambient Credential Prohibition Tests"
  description: |
    Tests verifying agents cannot access ambient GitHub credentials and that
    violations emit PolicyViolation with session quarantine.
  source_acceptance_test: FAC-AT-0011
  capture:
    paths:
      - "test/fac/credential_policy/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- policy::ambient_credentials"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Policy fixtures include denied credential access attempts"
  assertions:
    - "PolicyViolation emitted for ambient credential access"
    - "Session quarantined on credential access attempt"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
