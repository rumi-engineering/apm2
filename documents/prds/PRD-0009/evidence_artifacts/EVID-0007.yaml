prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0007"
  category: "INTEGRATION_TEST_RESULTS"
  title: "Gate Lease Scope and HTF Expiry Enforcement Tests"
  description: |
    Tests verifying lease scope immutability, HTF expiry enforcement, and GateRunCompleted
    binding to lease_id and changeset_digest.
  source_acceptance_test: FAC-AT-0007
  capture:
    paths:
      - "test/fac/lease_enforcement/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- lease::scope_and_expiry"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Lease fixtures include time_envelope_ref and scope variants"
          - "GateRunCompleted fixtures reference lease_id and changeset_digest"
  assertions:
    - "Expired leases (HTF) are rejected"
    - "Child leases cannot broaden parent scope"
    - "GateRunCompleted must reference lease_id and changeset_digest"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
