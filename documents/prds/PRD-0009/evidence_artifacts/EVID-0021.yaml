prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0021"
  category: "INTEGRATION_TEST_RESULTS"
  title: "Nondeterminism Quarantine and Abort Policy Tests"
  description: |
    Tests verifying quarantine and abort behavior when gate outcomes are nondeterministic.
  source_acceptance_test: FAC-AT-0021
  capture:
    paths:
      - "test/fac/quarantine_abort/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- determinism::quarantine_abort"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 180
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Work transition fixtures include BLOCKED rationale=QUARANTINED_CHANGESET"
          - "WorkAborted fixtures include abort_reason=POLICY_DENY"
  assertions:
    - "Nondeterminism triggers WorkTransitioned to BLOCKED with rationale QUARANTINED_CHANGESET"
    - "Repeated nondeterminism triggers WorkAborted abort_reason=POLICY_DENY"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
