prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0014"
  category: "LOAD_TEST_RESULTS"
  title: "FAC Reliability Soak Tests"
  description: |
    Soak tests verifying long-running gate execution and projection sync reliability
    under sustained load.
  source_acceptance_test: FAC-AT-0014
  capture:
    paths:
      - "test/fac/reliability/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- reliability::soak"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 600
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Load profile fixture includes concurrent GateRuns"
          - "Projection sync fixtures include repeated idempotent updates"
  assertions:
    - "No unbounded retries or resource leaks under sustained load"
    - "Gate and projection operations remain within budget limits"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
