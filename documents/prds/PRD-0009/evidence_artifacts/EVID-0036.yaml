prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-0036"
  category: "TEST_RESULTS"
  title: "GateReceipt Canonicalization and Versioning Tests"
  description: |
    Tests verifying GateReceipt envelope versioning, canonicalization rules, and
    rejection of unknown payload kinds/versions in enforce mode.
  source_acceptance_test: FAC-AT-0036
  capture:
    paths:
      - "test/fac/gate_receipt_canonicalization/"
  verification:
    commands:
      - id: "CMD-0001"
        shell: "bash"
        command: "cargo test --workspace -- gate_receipt::canonicalization_vectors"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 120
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Canonical bytes test vectors exist for receipt payloads"
          - "Unknown payload_schema_version rejected in enforce mode"
  assertions:
    - "Canonicalization mismatch fails verification"
    - "Unknown payload_kind/version rejected"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
