prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0009
  category: TEST_RESULTS
  title: MergeReceipt event binding and structure tests
  capture:
    paths:
    - tests/merge_receipt/
  verification:
    commands:
    - id: CMD-0009
      shell: bash
      command: cargo test --package apm2-core --lib work::merge::receipt::tests
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Tests must verify MergeReceipt binds Plan-of-Record digest.
      - Tests must verify MergeReceipt binds ChangeSet digest.
      - Tests must verify MergeReceipt binds all GateRun digests.
      - Tests must verify MergeReceipt binds all approval Decision digests.
      - Tests must verify MergeReceipt binds output repository state digest.
      - Verify MergeReceipt replaces PR merge status as canonical acceptance fact.
