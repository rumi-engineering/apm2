prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0006
  category: TEST_RESULTS
  title: Gate passage verification tests
  capture:
    paths:
    - tests/gate_execution/
  verification:
    commands:
    - id: CMD-0006
      shell: bash
      command: cargo test --package apm2-core --lib work::gate::tests
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 900
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Tests must verify gate execution emits structured Finding objects.
      - Verify evidence bundle validation before GateRunCompleted event.
      - Include tests for all gate outcomes (passed, failed, blocked).
