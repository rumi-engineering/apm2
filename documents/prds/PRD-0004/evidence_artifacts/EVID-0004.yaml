prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0004
  category: PROPERTY_TESTS
  title: FindingSignature collision and determinism property tests
  capture:
    paths:
    - tests/finding_signature/
  verification:
    commands:
    - id: CMD-0004
      shell: bash
      command: cargo test --package apm2-core --lib work::finding::signature::tests --include-ignored
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 1200
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Property tests must verify collision resistance across synthetic corpus.
      - Include tests for signature determinism with equivalent code patterns.
      - Verify blake3 fingerprint stability across normalization scenarios.
