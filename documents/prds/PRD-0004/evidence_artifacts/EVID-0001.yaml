prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0001
  category: TEST_RESULTS
  title: Plan lifecycle and spec snapshot tests
  capture:
    paths:
    - tests/plan_lifecycle/
  verification:
    commands:
    - id: CMD-0001
      shell: bash
      command: cargo test --package apm2-core --lib work::plan::tests --package apm2-core --lib work::spec::tests
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Ensure test outputs contain no secrets or confidential implementation details.
