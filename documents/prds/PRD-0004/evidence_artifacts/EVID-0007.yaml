prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0007
  category: TEST_RESULTS
  title: Divergence detection and reconciliation tests
  capture:
    paths:
    - tests/sync_reconciliation/
  verification:
    commands:
    - id: CMD-0007
      shell: bash
      command: cargo test --package apm2-adapters --lib github::divergence::tests
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 900
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Tests must verify divergence detection between ledger and GitHub state.
      - Include auto-reconciliation scenarios for common divergence patterns.
      - Verify sync adapter handles GitHub outages with bounded latency SLO.
