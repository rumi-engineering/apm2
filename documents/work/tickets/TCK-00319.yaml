ticket_meta:
  schema_version: '2026-01-24'
  template_version: '2026-01-24'
  ticket:
    id: TCK-00319
    title: Root tool handlers per workspace
  binds:
    prd_id: PRD-0010
    rfc_id: RFC-0019
    requirements:
    - requirement_id: REQ-0003
      requirement_ref: documents/rfcs/RFC-0019/requirements/REQ-0003.yaml#requirement
    evidence_artifacts:
    - evidence_id: EVID-0003
      artifact_ref: documents/rfcs/RFC-0019/evidence_artifacts/EVID-0003.yaml#evidence_artifact
  custody:
    agent_roles:
    - AGENT_IMPLEMENTER
    responsibility_domains:
    - DOMAIN_DAEMON
    - DOMAIN_SECURITY
  dependencies:
    tickets:
    - ticket_id: TCK-00318
      reason: Prerequisite per RFC-0019 ticket decomposition ordering
  scope:
    in_scope:
    - Amend ListFiles/Search handlers to use workspace root
    - Amend GitOperation handlers similarly
    - Path traversal prevention
    - 'TOCTOU race mitigation: use O_NOFOLLOW or fstat verification (RSK-0005)'
    out_of_scope:
    - Workspace creation (TCK-00318)
  plan:
    steps:
    - Read the bound requirement(s) and this ticket scope; confirm no scope creep beyond out_of_scope
      list.
    - 'Implement: Amend ListFiles/Search handlers to use workspace root'
    - 'Implement: Amend GitOperation handlers similarly'
    - 'Implement: Path traversal prevention'
    - 'Implement: TOCTOU race mitigation: use O_NOFOLLOW or fstat verification (RSK-0005)'
    - Add/adjust unit/integration tests to cover acceptance criteria and regression risks per
      RFC-0019/07_test_and_evidence.yaml.
    - Capture evidence artifacts (logs, receipts, test outputs) and update the corresponding EVID-*
      artifact with concrete artifact pointers.
  definition_of_done:
    evidence_ids:
    - EVID-0003
    criteria:
    - Tool handlers rooted to workspace, not daemon CWD
    - Symlink escape blocked
    - TOCTOU race mitigated via O_NOFOLLOW or fstat verification
    - ListFiles/Search reflect patched workspace
  notes:
    security: Productivity-first; keep fail-closed invariants intact where already mandated by
      RFC-0019.
    verification: Prefer deterministic tests and CAS/ledger replay checks; avoid relying on process
      memory.
    general: Keep changes minimal, explicit, and reversible; make dependencies and attribution
      fields queryable.
