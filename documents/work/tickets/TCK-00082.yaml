ticket_meta:
  schema_version: "2026-01-24"
  template_version: "2026-01-24"
  ticket:
    id: "TCK-00082"
    title: "Document cargo-nextest in CONTRIBUTING.md"
  binds:
    prd_id: "NONE"
    rfc_id: "RFC-0007"
    requirements:
      - requirement_id: "PERF-003"
        requirement_ref: "documents/rfcs/RFC-0007/01_problem_and_imports.yaml#rfc_local_requirements.requirements[2]"
    evidence_artifacts:
      - evidence_id: "EVID-7003"
        artifact_ref: "CONTRIBUTING.md#faster-test-runner-cargo-nextest"
  custody:
    agent_roles:
      - "AGENT_IMPLEMENTER"
    responsibility_domains:
      - "DOMAIN_DEVELOPER_EXPERIENCE"
  dependencies:
    tickets:
      - "TCK-00080"
  scope:
    in_scope:
      - "Add cargo-nextest subsection to Build Optimization section"
      - "Document installation: cargo install cargo-nextest"
      - "Document usage: cargo nextest run"
      - "Explain performance benefits (2-3x faster)"
      - "Explain process-level parallelism"
      - "Note compatibility with cargo test"
    out_of_scope:
      - "Mandating nextest in CI"
      - "Custom nextest configuration (.config/nextest.toml)"
      - "nextest profiles"
      - "Detailed nextest feature documentation"
  plan:
    steps:
      - "Read current CONTRIBUTING.md structure (after TCK-00081)"
      - "Add '### Faster Test Runner (cargo-nextest)' subsection"
      - "Document what nextest is and performance benefits"
      - "Document installation: cargo install cargo-nextest"
      - "Document usage: cargo nextest run"
      - "Explain process-level parallelism vs cargo test's thread-level"
      - "Note that cargo test remains fully supported"
      - "Verify documentation is accurate"
  definition_of_done:
    evidence_ids:
      - "EVID-7003"
    criteria:
      - "CONTRIBUTING.md has cargo-nextest subsection"
      - "Installation command is documented: cargo install cargo-nextest"
      - "Usage is documented: cargo nextest run"
      - "Performance benefits are explained (2-3x faster)"
      - "Process-level parallelism is explained"
      - "Compatibility with cargo test is noted"
  notes:
    security: |
      No security implications. cargo-nextest runs the same tests as cargo test
      with the same permissions. It does not introduce any new attack surface.
    implementation_details: |
      Documentation content to add:

      ```markdown
      ### Faster Test Runner (cargo-nextest)

      [cargo-nextest](https://nexte.st/) is a next-generation test runner that
      executes tests significantly faster than `cargo test` by running tests
      in parallel at the process level.

      **Installation:**
      ```bash
      cargo install cargo-nextest
      ```

      **Usage:**
      ```bash
      cargo nextest run                    # Run all tests
      cargo nextest run -p apm2-core       # Run tests for specific crate
      cargo nextest run --workspace        # Run all workspace tests
      cargo nextest run -- test_name       # Run specific test by name
      ```

      **Why it's faster:**
      - `cargo test` runs tests in parallel within each test binary (threads)
      - `cargo-nextest` runs test binaries in parallel (processes)
      - This provides better isolation and typically 2-3x faster execution

      **Note:** `cargo test` remains fully supported. nextest is recommended
      for faster local development but is not required.
      ```
    affected_files: |
      Modified: CONTRIBUTING.md (add cargo-nextest documentation)
