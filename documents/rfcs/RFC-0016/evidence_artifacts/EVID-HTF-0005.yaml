rfc_evidence_artifact:
  schema_version: "2026-01-30"
  id: "EVID-HTF-0005"
  category: "LINT_REPORTS"
  title: "Normative doc time-unit lint"
  description: |
    Evidence that the doc-lint rule forbidding human-time units in normative
    artifacts is enforced, with a narrow external-facing escape hatch.
  capture:
    paths:
      - "htf/lint/"
  verification:
    commands:
      - id: "CMD-HTF-0005"
        shell: "bash"
        command: "cargo xtask lint --include-docs"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 600
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Scoped docs without explicit external marker are rejected when they contain forbidden human durations"
          - "External marker escape hatch is allowed"
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
