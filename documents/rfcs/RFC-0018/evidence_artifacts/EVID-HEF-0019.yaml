rfc_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-HEF-0019"
  category: "PERFORMANCE_RESULTS"
  title: "Digest-first scale/evidence-economics validation"
  capture:
    paths:
      - "evidence/rfc/RFC-0018/workobject_cutover/scale_economics/"
  verification:
    commands:
      - id: "CMD-HEF-0019"
        shell: "bash"
        command: "cargo test -p apm2-daemon --tests fac_digest_first_scale -- --nocapture"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 2400
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Supervisor surfaces must remain summary-first with selector-based evidence retrieval."
          - "Replay/anti-entropy drills must recover projection state without duplicate side-effects."
          - "Control-plane payload growth must stay within configured queue/payload budgets."
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
