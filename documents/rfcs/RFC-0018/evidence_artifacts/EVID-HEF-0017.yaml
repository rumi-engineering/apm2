rfc_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-HEF-0017"
  category: "INTEGRATION_TEST_RESULTS"
  title: "xtask staged demotion and instruction-plane conformance"
  capture:
    paths:
      - "evidence/rfc/RFC-0018/workobject_cutover/xtask_demotion/"
  verification:
    commands:
      - id: "CMD-HEF-0017"
        shell: "bash"
        command: "cargo test -p xtask -- --nocapture && bash scripts/lint/check_lifecycle_instruction_surface.sh"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 1800
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "Stage-2 default must not execute direct lifecycle write paths."
          - "Instruction/prompt surfaces must not include deprecated lifecycle control commands."
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
