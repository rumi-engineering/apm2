rfc_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: "EVID-HEF-0017"
  category: "INTEGRATION_TEST_RESULTS"
  title: "xtask staged demotion and instruction-plane conformance"
  capture:
    paths:
      - "evidence/rfc/RFC-0018/workobject_cutover/xtask_demotion/"
  verification:
    commands:
      - id: "CMD-HEF-0017"
        shell: "bash"
        command: "bash scripts/lint/check_lifecycle_instruction_surface.sh && bash scripts/lint/no_direct_status_write_commands.sh && cargo test --workspace"
        expected_exit_code: 0
        cwd: "."
        timeout_seconds: 1800
        network_access: "DISALLOWED"
        allowlist_hosts: []
        constraints:
          - "xtask crate fully removed (Stage-3 complete); lifecycle authority resides in apm2 fac CLI."
          - "Instruction/prompt surfaces must not include deprecated lifecycle control commands."
          - "CI lint guards prevent reintroduction of cargo xtask lifecycle strings."
    notes:
      - "Original command `cargo test -p xtask` superseded by xtask crate removal (PR #516). Workspace tests and lint scripts now constitute the verification surface."
  data:
    classification: "INTERNAL"
    redaction:
      required: false
      guidance: []
