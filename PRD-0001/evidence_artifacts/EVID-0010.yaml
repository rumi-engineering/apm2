prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0010
  category: TEST_RESULTS
  title: Content-addressed artifact store verification outputs
  capture:
    paths:
    - tests/artifact_cas/
  verification:
    commands:
    - id: CMD-0010
      shell: bash
      command: ./scripts/evidence/verify_artifact_cas.sh
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 900
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Artifact store tests must use synthetic payloads and include hash verification
        outputs.
