prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0009
  category: TEST_RESULTS
  title: Offline local inference execution evidence (network disabled)
  capture:
    paths:
    - tests/offline_inference/
  verification:
    commands:
    - id: CMD-0009
      shell: bash
      command: ./scripts/evidence/verify_offline_inference.sh --network disabled
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 1800
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Do not log prompts containing secrets; apply prompt redaction policy to captured
        transcripts.
