prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0002
  category: LINT_REPORTS
  title: Template suite lint report for PRD/RFC/tickets
  capture:
    paths:
    - lint/
  verification:
    commands:
    - id: CMD-0002
      shell: bash
      command: ./scripts/evidence/verify_template_lint.sh --root . --prd PRD-0001
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Ensure lint outputs contain no secrets; redact file paths if they reveal confidential
        repository structure.
