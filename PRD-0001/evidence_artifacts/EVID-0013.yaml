prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0013
  category: THREAT_MODEL_ARTIFACTS
  title: Threat model and attack surface analysis for APM2 kernel
  capture:
    paths:
    - security/threat_model/
  verification:
    commands:
    - id: CMD-0013
      shell: bash
      command: ./scripts/evidence/verify_threat_model_present.sh --prd PRD-0001
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 300
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Threat model should not include exploitable secrets; redact sensitive infrastructure
        details.
