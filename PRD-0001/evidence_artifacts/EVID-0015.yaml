prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0015
  category: SBOM
  title: SBOM generation outputs for release artifacts
  capture:
    paths:
    - release/sbom/
  verification:
    commands:
    - id: CMD-0015
      shell: bash
      command: ./scripts/evidence/verify_sbom.sh --format spdx-json
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 600
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - SBOMs may reveal dependency graph; treat as INTERNAL unless otherwise approved.
