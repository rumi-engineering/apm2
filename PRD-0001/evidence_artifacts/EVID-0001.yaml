prd_evidence_artifact:
  schema_version: "2026-01-23"
  template_version: "2026-01-23"
  id: EVID-0001
  category: RECORDINGS
  title: End-to-end autonomous factory run transcript and artifacts
  capture:
    paths:
    - runs/e2e_factory_run/
  verification:
    commands:
    - id: CMD-0001
      shell: bash
      command: ./scripts/evidence/verify_e2e_factory_run.sh evidence/prd/PRD-0001/runs/e2e_factory_run
      expected_exit_code: 0
      cwd: .
      timeout_seconds: 1200
      network_access: DISALLOWED
  data:
    classification: INTERNAL
    redaction:
      required: false
      guidance:
      - Redact secrets/tokens from transcripts before publication; store raw logs
        as RESTRICTED if needed.
