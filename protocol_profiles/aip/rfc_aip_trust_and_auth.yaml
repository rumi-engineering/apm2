aip_rfc_trust_and_auth:
  schema_version: "2026-01-23"
  template_version: 2
  trust_boundary_overrides:
    - trust_boundary_id: "TB-0001"
      requires:
        authn: "mTLS OR signed requests with nonce + timestamp"
        authz: "capability tokens scoped to message types"
        replay_protection: true
        key_management: "rotated keys; no long-lived shared secrets"
      required_evidence_ids:
        - "EVID-0901"
        - "EVID-0902"
