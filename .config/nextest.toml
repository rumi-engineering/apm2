# cargo-nextest configuration
# https://nexte.st/docs/configuration/

[profile.default]
# Per-test timeout: 10 minutes.
slow-timeout = { period = "60s", terminate-after = 10 }
fail-fast = false

[profile.ci]
# CI uses the same 10-minute per-test timeout.
slow-timeout = { period = "60s", terminate-after = 10 }
fail-fast = true
